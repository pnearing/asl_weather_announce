#!/usr/bin/make -f

export DH_VERBOSE = 1

%:
	dh $@ --with=systemd

# Do not enable the timer by default - let the user decide when to enable it
override_dh_systemd_enable:
	dh_systemd_enable --no-enable

# Do not start the timer by default
override_dh_systemd_start:
	dh_systemd_start --no-start

